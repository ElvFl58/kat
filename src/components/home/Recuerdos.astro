<section class="section-amor">
  <h1 class="titulo-neon">🌹 Recuerdos 🌹</h1>

  <div class="mensaje">
    Eres mi inspiración, mi
    alegría, mi paz. <br />
    Atte: .. 💕
  </div>

  <div class="slider-romantico">
    <div class="slides">
      <div class="slide" style="--bg:url('/img/recuerdos/001.jpeg')">
        <img src="/img/recuerdos/001.jpeg" alt="Momento 01" />
      </div>
      <div class="slide active" style="--bg:url('/img/kat/03.jpg')">
        <img src="/img/kat/03.jpg" alt="Momento 1" />
      </div>
      <div class="slide" style="--bg:url('/img/kat/kat.jpg')">
        <img src="/img/kat/kat.jpg" alt="Momento 2" />
      </div>
      <div class="slide" style="--bg:url('/img/recuerdos/foto.jpg')">
        <img src="/img/recuerdos/foto.jpg" alt="Momento 3" />
      </div>
      <div class="slide" style="--bg:url('/img/recuerdos/cachimbos.jpg')">
        <img src="/img/recuerdos/cachimbos.jpg" alt="Momento 4" />
      </div>
      <div class="slide" style="--bg:url('/img/recuerdos/foto2.jpg')">
        <img src="/img/recuerdos/foto2.jpg" alt="Momento 5" />
      </div>
      <div class="slide" style="--bg:url('/img/recuerdos/foto3.jpg')">
        <img src="/img/recuerdos/foto3.jpg" alt="Momento 6" />
      </div>
    </div>

    <!-- Controles -->
    <button class="control prev">❮</button>
    <button class="control next">❯</button>

    <!-- Indicadores -->
    <div class="indicators"></div>
  </div>
</section>

<style>
  /* Fondo romántico animado */
  .section-amor {
    padding: 40px 20px;
    color: white;
    text-align: center;
    background: linear-gradient(-45deg, #aa00ff, #ff6fec, #d11749, #c03c7e);
    background-size: 400% 400%;
    animation: fondoAnim 20s ease infinite;
    position: relative;
    overflow: hidden;
  }
  @keyframes fondoAnim {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  /* Destellos románticos */
  .section-amor::before {
    content: "";
    position: absolute;
    inset: 0;
    background: radial-gradient(
      circle,
      rgba(255, 255, 255, 0.15) 1px,
      transparent 1px
    );
    background-size: 50px 50px;
    animation: twinkle 8s infinite linear;
  }
  @keyframes twinkle {
    0%,
    100% {
      transform: translateY(0);
      opacity: 0.9;
    }
    50% {
      transform: translateY(-15px);
      opacity: 1;
    }
  }

  /* Título neón */
  .titulo-neon {
    font-size: 2.8em;
    margin-bottom: 25px;
    text-shadow:
      0 0 10px #890ba8,
      0 0 20px #ff6fec,
      0 0 40px #ff6fec,
      0 0 80px #ff6fec,
      0 0 100px #ff6fec;
    position: relative;
    z-index: 2;
    animation: glowTitle 2.5s infinite alternate;
  }
  @keyframes glowTitle {
    from {
      text-shadow:
        0 0 10px #890ba8,
        0 0 20px #ff6fec;
    }
    to {
      text-shadow:
        0 0 15px #ff6fec,
        0 0 35px #ff6fec;
    }
  }

  /* Mensaje */
  .mensaje {
    font-size: 1.3em;
    max-width: 700px;
    margin: 0 auto 35px;
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.7);
    line-height: 1.6;
    position: relative;
    z-index: 2;
    animation: fadeIn 2s ease forwards;
  }

  /* Slider */
  .slider-romantico {
    position: relative;
    max-width: 900px;
    margin: auto;
    overflow: hidden;
    border-radius: 20px;
    box-shadow: 0 0 50px rgba(255, 105, 180, 0.4);
  }
  .slides {
    position: relative;
    width: 100%;
    height: 70vh;
  }
  .slide {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--bg) center/cover no-repeat;
    opacity: 0;
    transform: scale(1.1);
    transition:
      opacity 1.2s ease,
      transform 1.5s ease;
  }
  .slide img {
    max-width: 100%;
    max-height: 100%;
    object-fit: cover;
    border-radius: 18px;
    box-shadow: 0 0 35px rgba(255, 105, 180, 0.5);
    transition: transform 8s ease;
  }
  .slide.active img {
    transform: scale(1.05);
  }
  .slide.active {
    opacity: 1;
    transform: scale(1);
    z-index: 2;
  }

  /* Controles */
  .control {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(255, 255, 255, 0.6);
    border: none;
    font-size: 2rem;
    padding: 12px;
    border-radius: 50%;
    cursor: pointer;
    z-index: 3;
    transition:
      background 0.3s,
      transform 0.3s;
  }
  .control:hover {
    background: rgba(255, 255, 255, 0.9);
    transform: scale(1.1);
  }
  .control.prev {
    left: 12px;
  }
  .control.next {
    right: 12px;
  }

  /* Indicadores */
  .indicators {
    position: absolute;
    bottom: 15px;
    width: 100%;
    display: flex;
    justify-content: center;
    gap: 10px;
    z-index: 3;
  }
  .indicators span {
    width: 14px;
    height: 14px;
    background: rgba(255, 255, 255, 0.6);
    border-radius: 50%;
    cursor: pointer;
    transition:
      transform 0.3s,
      background 0.3s;
  }
  .indicators span.active {
    background: #ff6fec;
    transform: scale(1.4);
  }

  /* Animaciones */
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(15px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive */
  @media (max-width: 768px) {
    .slides {
      height: 50vh;
    }
    .titulo-neon {
      font-size: 2.2em;
    }
    .mensaje {
      font-size: 1.1em;
    }
  }
</style>
