<section class="section-romantica">
  <h2>KAT ❤️</h2>

  <div class="heart-animation-container">
    <div class="animated-heart">💖</div>
  </div>

  <div class="te-amo-animation">Te Amo Katy</div>

  <!-- Corazones flotando -->
  <div class="corazones-flotantes">
    <span>❤️</span>
    <span>💘</span>
    <span>💝</span>
    <span>💖</span>
    <span>💗</span>
  </div>
</section>
<style>
  .section-romantica {
    background-image: url("/img/kat/kat.jpg");
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    padding: 60px 20px;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .section-romantica::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.4);
    z-index: 0;
  }

  .section-romantica h2 {
    font-weight: bold;
    font-size: clamp(2rem, 8vw, 4rem);
    color: white;
    position: relative;
    z-index: 1;
    text-shadow:
      0 0 15px #ff69b4,
      0 0 30px #ff1493;
    animation: fadeInDown 2s ease-out;
  }

  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .heart-animation-container {
    display: flex;
    justify-content: center;
    margin-top: 20px;
    position: relative;
    z-index: 1;
  }

  .animated-heart {
    font-size: 15vw;
    animation: pulseHeart 1.5s infinite alternate;
    text-shadow: 0 0 20px rgba(255, 105, 180, 0.7);
  }

  @keyframes pulseHeart {
    0% {
      transform: scale(0.9);
    }
    100% {
      transform: scale(1.3);
    }
  }

  .te-amo-animation {
    font-style: italic;
    font-size: clamp(1.5rem, 5vw, 3rem);
    color: #ffdde1;
    text-shadow: 0 0 10px rgba(255, 182, 193, 0.8);
    opacity: 0;
    animation: aparecerDesaparecer 6s infinite;
    position: relative;
    z-index: 1;
    margin-top: 15px;
  }

  @keyframes aparecerDesaparecer {
    0%,
    80% {
      opacity: 0;
    }
    90% {
      opacity: 1;
    }
    100% {
      opacity: 0;
    }
  }

  /* Corazones flotantes */
  .corazones-flotantes {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    pointer-events: none;
  }

  .corazones-flotantes span {
    position: absolute;
    font-size: 2rem;
    animation: flotar 6s linear infinite;
    opacity: 0.8;
  }

  .corazones-flotantes span:nth-child(1) {
    left: 10%;
    animation-delay: 0s;
  }
  .corazones-flotantes span:nth-child(2) {
    left: 30%;
    animation-delay: 1s;
  }
  .corazones-flotantes span:nth-child(3) {
    left: 50%;
    animation-delay: 2s;
  }
  .corazones-flotantes span:nth-child(4) {
    left: 70%;
    animation-delay: 3s;
  }
  .corazones-flotantes span:nth-child(5) {
    left: 90%;
    animation-delay: 4s;
  }

  @keyframes flotar {
    0% {
      transform: translateY(100%);
      opacity: 0;
    }
    20% {
      opacity: 1;
    }
    100% {
      transform: translateY(-100%);
      opacity: 0;
    }
  }
</style>
