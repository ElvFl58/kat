---
import "../styles/global.css";
import Navbar from "../components/shared/Navbar.astro";
import Footer from "../components/shared/Footer.astro";
import Modal from "../components/Modal.jsx";
interface Props {
  title: string;
}
const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="shortcut icon" href="favicon.svg" type="image/x-icon" />
    <!-- <link rel="icon" type="image/svg+xml" href="/descarga.svg" />
    <meta name="generator" content={Astro.generator} /> -->
    <title>{title}</title>
    <link rel="stylesheet" href="/src/styles/global.css" />
    <style>
      /* body {
        margin: 0;
        height: 100vh;
        background: linear-gradient(to top, #0f2027, #203a43, #2c5364);
        overflow: hidden;
        font-family: Arial, sans-serif;
      } */

      .heart {
        position: absolute;
        bottom: -20px;
        font-size: 20px;
        color: rgba(255, 80, 120, 0.9);
        animation: floatUp linear forwards;
        user-select: none;
        pointer-events: none;
      }

      @keyframes floatUp {
        0% {
          transform: translateY(0) scale(1);
          opacity: 0;
        }

        10% {
          opacity: 1;
        }

        100% {
          transform: translateY(-110vh) scale(1.6);
          opacity: 0;
        }
      }
    </style>
  </head>
  <body>
    <div class="bg-pink-400">
      <Navbar />
      <slot />
      <Footer />
      <Modal client:load />
    </div>
  </body>
  <script type="text/javascript">
    // function createHeart() {
    //   const heart = document.createElement("div");
    //   heart.classList.add("heart");
    //   heart.innerHTML = "🎄🎅";

    //   heart.style.left = Math.random() * 100 + "vw";
    //   heart.style.fontSize = Math.random() * 20 + 20 + "px";

    //   const duration = Math.random() * 3 + 4;
    //   heart.style.animationDuration = duration + "s";

    //   document.body.appendChild(heart);

    //   setTimeout(() => {
    //     heart.remove();
    //   }, duration * 1000);
    // }
    // // ====== BLOQUEAR SCROLL ======
    // document.body.style.overflow = "hidden";

    // // ====== NIEVE ======
    // for (let i = 0; i < 60; i++) {
    //   const snow = document.createElement("div");
    //   snow.className = "snow";
    //   snow.innerHTML = "❄";
    //   snow.style.left = Math.random() * 100 + "vw";
    //   snow.style.fontSize = Math.random() * 10 + 10 + "px";
    //   snow.style.animationDuration = Math.random() * 3 + 3 + "s";
    //   document.getElementById("christmasIntro").appendChild(snow);
    // }
  </script>

  <style>
    html,
    body {
      cursor:
        url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"><path fill="%23ff0066" d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/><animateTransform attributeName="transform" type="scale" values="1;1.2;1" dur="0.8s" repeatCount="indefinite"/></svg>'),
        auto;
      margin: 0;
      margin: 0;
      width: 100%;
      height: 100%;
    }
  </style>
</html>
