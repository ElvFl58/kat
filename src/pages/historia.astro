---
import Layout from "../layouts/Layout.astro";

const logros = [
 /** {
    year: "21 - 09 - 2023",
    title: "Un nuevo sueño 💍",
    desc: "Se prepara para cumplir nuevos sueños, uno a la vez, con amor.",
    img: "/img/kat/kat2.jpg",
  },
  {
    year: "2025",
    title: "Viaje a Bolivia 🏞️",
    desc: "Aventura inolvidable al lago Titicaca, compartiendo paisajes y sonrisas.",
    img: "/img/history/viaje.png",
  },
  {
    year: "2025",
    title: "Fiest de Ingreso",
    desc: "Fiesta de cachimbos por haber ingresado.",
    img: "/img/history/welcome.jpg",
  },*/
  {
    year: "2025",
    title: "Viaje",
    desc: "Aventura inolvidable",
    img: "/img/historia/03.png",
  },
  {
    year: "2023",
    title: "Fiesta de Ingreso",
    desc: "Dia inolvidadble",
    img: "/img/historia/01.jpg",
  },
  {
    year: "2023",
    title: "Ingreso a la Universidad 🎓",
    desc: "Kat empezó su gran aventura universitaria, llena de sueños y pasión.",
    img: "/img/historia/02.jpg",
  },
];
---

<Layout title="Nuestra Historia">
  <main
    class="bg-gradient-to-br from-pink-50 via-purple-100 to-pink-200 min-h-screen font-['Poppins'] flex items-center justify-center overflow-hidden"
  >
    <section
      class="container mx-auto px-4 sm:px-6 lg:px-8 py-12 sm:py-16 lg:py-20 flex flex-col items-center justify-center w-full max-w-7xl"
    >
      <!-- Encabezado romántico -->
      <header class="max-w-4xl text-center mb-12 sm:mb-16 w-full text-4xl">
        <h1
          class="text-6xl sm:text-5xl md:text-6xl lg:text-7xl font-['Dancing_Script'] font-bold text-pink-500 drop-shadow-md mb-4 sm:mb-6 animate-fade-in"
        >
           Historia 💞
        </h1>
        <p
          class="w-auto text-lg sm:text-xl text-purple-700 italic leading-relaxed animate-fade-in-slow px-4"
        >
          Un sendero tejido con amor, sueños y promesas. Cada recuerdo, una
          flor. Cada día, una nueva página escrita con el corazón.
        </p>
      </header>

      <!-- Línea del tiempo refinada con contenedor mejorado -->
      <article
        class="relative timeline w-full max-w-6xl mx-auto flex-1 flex items-center justify-center overflow-visible"
      >
        <div
          class="space-y-16 sm:space-y-20 lg:space-y-24 relative z-10 w-full px-4 sm:px-6 lg:px-8"
        >
          {
            logros.map((item, i) => {
              const position =
                i % 2 === 0 ? "md:flex-row" : "md:flex-row-reverse";
              return (
                <div
                  class={`flex flex-col ${position} items-center md:items-start md:gap-8 lg:gap-10 justify-center py-9 sm:py-9 w-full max-w-5xl mx-auto text-center`}
                >
                  {/* Imagen circular con contenedor mejorado */}
                  <div
                    class="p-9 sm:p-4 w-24 h-24 sm:w-28 sm:h-28 md:w-32 md:h-32 lg:w-36 lg:h-36 rounded-full overflow-hidden border-4 border-pink-400 shadow-xl bg-pink-400 flex-shrink-0 group cursor-pointer transition-transform duration-300 hover:scale-110 flex items-center justify-center "
                    onclick={`openModal('${item.img}', '${item.title}')`}
                  >
                    <img
                      src={item.img}
                      alt={item.title}
                      class="w-full h-full object-cover rounded-full"
                      onerror="this.onerror=null;this.src='https://placehold.co/144x144?text=Kat';"
                    />
                  </div>

                  {/* Caja de contenido con contenedor mejorado */}
                  <div class="bg-white/95 backdrop-blur-md shadow-lg rounded-3xl border-l-8 border-pink-400 w-full max-w-md sm:max-w-lg lg:max-w-xl px-6 sm:px-8 py-6 sm:py-8 transition-transform duration-300 hover:scale-105 flex-shrink-0">
                    <span class="text-sm sm:text-base text-purple-600 font-semibold tracking-wide block mb-2">
                      {item.year}
                    </span>
                    <h3 class="text-xl sm:text-2xl lg:text-3xl text-pink-600 font-['Dancing_Script'] font-bold mb-3 leading-tight">
                      {item.title}
                    </h3>
                    <p class="text-sm sm:text-base text-gray-700 leading-relaxed italic">
                      {item.desc}
                    </p>
                  </div>
                </div>
              );
            })
          }
        </div>
      </article>

      <!-- Pie romántico con contenedor mejorado -->
      <footer
        class="max-w-3xl mx-auto text-center mt-16 sm:mt-20 lg:mt-24 animate-fade-in-slow w-full px-4"
      >
        <div
          class="bg-white/80 backdrop-blur-xl rounded-3xl p-8 sm:p-10 shadow-xl border border-pink-300"
        >
          <h2
            class="text-3xl sm:text-4xl font-['Dancing_Script'] text-pink-600 mb-4"
          >
            🌸 Nuestro amor no tiene fin 🌸
          </h2>
          <p
            class="text-purple-700 text-base sm:text-lg italic leading-relaxed"
          >
            Cada mirada, cada abrazo, cada promesa... sigue floreciendo. Nuestra
            historia no termina, solo evoluciona.
          </p>
        </div>
      </footer>
    </section>
  </main>

  <!-- Modal refinado -->
  <div
    id="imageModal"
    class="fixed inset-0 bg-pink-300/40 backdrop-blur-sm z-50 hidden flex items-center justify-center p-4 transition-opacity duration-300"
  >
    <div class="relative max-w-4xl max-h-full animate-fade-in w-full">
      <img
        id="modalImage"
        src=""
        alt=""
        class="w-full h-auto max-h-[90vh] object-contain rounded-2xl shadow-2xl"
      />
      <div class="absolute top-4 right-4 bg-black rounded-4xl">
        <button
          onclick="closeModal()"
          class="bg-purple-600 text-white text-2xl rounded-full w-12 h-12 flex items-center justify-center hover:bg-purple-700 transition-all duration-300 shadow-lg"
        >
          ❌
        </button>
      </div>
      <div
        class="absolute bottom-4 left-4 bg-white/90 backdrop-blur-md text-purple-800 px-6 py-3 rounded-xl shadow-md"
      >
        <h3
          id="modalTitle"
          class="text-center text-2xl font-semibold font-['Dancing_Script']"
        >
        </h3>
      </div>
    </div>
  </div>

  <!-- Estilos extra -->
  <style>
    
    .timeline::before {
      content: "";
      position: absolute;
      left: 50%;
      top: 0;
      transform: translateX(-50%);
      width: 4px;
      height: 100%;
      background: linear-gradient(to bottom, #e879f9, #a855f7);
      z-index: 0;
      animation: growLine 2s ease-out forwards;
    }

    @keyframes growLine {
      0% {
        height: 0;
      }
      100% {
        height: 100%;
      }
    }

    /* Animaciones suaves */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .animate-fade-in {
      animation: fadeIn 1.2s ease forwards;
    }

    .animate-fade-in-slow {
      animation: fadeIn 2s ease forwards;
    }

    #imageModal.show {
      display: flex !important;
    }

    .group:hover {
      transform: scale(1.1);
      box-shadow: 0 12px 30px rgba(236, 72, 153, 0.4);
    }

    /* Responsive adjustments */
    @media (max-width: 768px) {
      .timeline::before {
        left: 20px;
      }

      .timeline > div > div {
        padding-left: 40px;
      }
    }
  </style>

  <!-- JS Modal -->
  <script type="text/javascript">
    function openModal(imgSrc, title) {
      const modal = document.getElementById("imageModal");
      const modalImage = document.getElementById("modalImage");
      const modalTitle = document.getElementById("modalTitle");

      modalImage.src = imgSrc;
      modalImage.alt = title;
      modalTitle.textContent = title;

      modal.classList.remove("hidden");
      modal.classList.add("show");

      document.addEventListener("keydown", function (e) {
        if (e.key === "Escape") {
          closeModal();
        }
      });

      modal.addEventListener("click", function (e) {
        if (e.target === modal) {
          closeModal();
        }
      });
    }

    function closeModal() {
      const modal = document.getElementById("imageModal");
      modal.classList.add("hidden");
      modal.classList.remove("show");
    }
  </script>
</Layout>
